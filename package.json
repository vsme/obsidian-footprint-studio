{
  "name": "obsidian-footprint-studio",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --external:obsidian --format=cjs --platform=browser --target=es2020 --outfile=main.js",
    "dev": "pnpm run build -- --watch"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "pinyin-pro": "^3.27.0"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.8.0"
  }
}
